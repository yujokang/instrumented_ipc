# A Framework for Communicating with an Instrumented Program

Inspired by AFL's LLVM mode (http://lcamtuf.coredump.cx/afl/),
although this framework
does not include optimizations such as repeated execution.
